Mohammed Bilal Ather
P2
------------------------------------------------------------------------
To compile: 

run `make`

To clean: 

run `make clean`

To run from input:

run `./staticSem`

to run from file

run `./staticSem < filename` OR

run `./staticSem filename`

NOTE: If a variable is declared twice or used without declaration, then this program will throw an error
